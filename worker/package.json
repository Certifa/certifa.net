{
  "name": "certifa-forms-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker — contact-form mail relay for certifa.net via Resend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.0.0"
  }
}
